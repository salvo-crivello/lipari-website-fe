name: Deploy UAT

on:
  push:
    branches: [deploy/uat]
  workflow_dispatch:

concurrency:
  group: deploy-uat
  cancel-in-progress: false

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - run: yarn lint
      - run: yarn tsc --noEmit
      - run: yarn test run
      - run: yarn build
        env:
          NEXT_PUBLIC_SITE_URL: ${{ vars.UAT_SITE_URL }}

  deploy:
    name: Deploy to UAT
    runs-on: ubuntu-latest
    needs: [validate]
    environment: uat
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup

      - name: Install Vercel CLI
        run: yarn dlx vercel@latest --version

      - name: Pull Vercel environment info
        run: yarn dlx vercel@latest pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Build via Vercel
        run: yarn dlx vercel@latest build --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Deploy prebuilt output to UAT
        run: yarn dlx vercel@latest deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
